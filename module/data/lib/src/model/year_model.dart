import 'dart:ui';

class YearModel{
  String year;
  Color color;
  String title;
  String description;
  String subText;
  YearModel({required this.year,required this.color,required this.title,required this.description,required this.subText});
}